export default function EventPage() {
  return (
    <main style={{ padding: "12px 0" }}>
      <h2 style={{ fontSize: 16, marginBottom: 8 }}>事件合约（迁移中）</h2>
      <p style={{ color: "var(--text-muted)", fontSize: 13, lineHeight: 1.6 }}>
        该页会把原 `pages/event.html` + `js/event.js` 的 UI 与策略推理逐步组件化，保留现有计算逻辑并增加更细的测试覆盖。
      </p>
    </main>
  );
}

